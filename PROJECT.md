# Школа 3D — техническая записка

Версия **1.0.0-beta** · дата сборки 2026-09-09.
Цель документа — чтобы проект можно было продолжать через месяц без перечитывания всего кода.

---

## 1. Принципы

1. **Ноль зависимостей в runtime.** В `package.json` есть только `typescript` в devDependencies.
   Рендер, физика, звук, кат-сцены, ИИ — свои.
2. **Одна ответственность на файл.** Геометрия не знает про геймплей, геймплей не знает про WebGL.
3. **Все числа — в `src/config.ts`.** Баланс правится в одном месте.
4. **Фиксированный шаг физики** 1/120 с, до 6 шагов на кадр — одинаковое поведение на 60 и 144 Гц
   и готовность к сетевой синхронизации.
5. **Ничего не создаётся в цикле кадра.** Буферы, массивы и меши переиспользуются, поэтому нет рывков от сборщика мусора.

---

## 2. Карта кода

| Файл | За что отвечает |
| --- | --- |
| `src/main.ts` | загрузка, меню, цикл кадра, горячие клавиши, параметры URL |
| `src/config.ts` | все константы: игрок, камера, свет, физика, хоррор, звук |
| `src/core/gl.ts` | создание контекста WebGL2, шейдеры, буферы |
| `src/core/shaders.ts` | GLSL: туман, солнце, запечённый свет, конус фонаря, множитель яркости |
| `src/core/renderer.ts` | чанки статики, динамические меши, отсев по фрустуму, адаптивное разрешение |
| `src/core/mesh.ts` | сборка геометрии: `box`, `rotatedBox`, `horizontalQuad`, шум, затенение |
| `src/core/clock.ts` | время кадра, фиксированные шаги, FPS |
| `src/core/input.ts` | клавиатура, захват мыши, чувствительность |
| `src/world/layout.ts` | разметка школы: комнаты, стены, дверные проёмы, точки старта |
| `src/world/school.ts` | генерация всей геометрии и коллайдеров |
| `src/world/props.ts` | парты, стулья, доски, шкафчики, турники, цветы |
| `src/world/collision.ts` | сетка AABB, `overlaps`, `groundHeight`, ступеньки |
| `src/player/controller.ts` | ходьба, бег, приседание, прыжок, качание головы |
| `src/entities/teacher.ts` | модель учительницы + конечный автомат поведения |
| `src/game/nav.ts` | граф навигации и поиск пути (A*) по школе |
| `src/game/timeline.ts` | движок кат-сцен: шаги, прогресс, сглаживание, пропуск |
| `src/game/items.ts` | описание предметов, их модели, предмет в руке, баррикада, двери, шкафчики |
| `src/game/game.ts` | главный мозг: состояния, кат-сцены, квест, жизни, ночь, звуки, HUD |
| `src/audio/audio.ts` | WebAudio: загрузка 29 буферов, циклы, панорама и затухание по расстоянию |
| `src/ui/menu.ts` | меню, панели, настройки и их сохранение в `localStorage` |
| `src/ui/gameHud.ts` | жизни, слоты предметов, подсказки, полоса удержания, субтитры, виньетка |
| `src/ui/overlay.ts` | загрузка, пауза, ошибка |
| `src/ui/minimap.ts` | 2D-миникарта на canvas |

---

## 3. Конвейер отрисовки

- Одна шейдерная программа, один формат вершины: позиция, нормаль, цвет, запечённый свет (10 float).
- Статика разбита на чанки по комнатам; каждый чанк — своё VAO и AABB, отсев по фрустуму.
- Динамика (учительница, предметы, предмет в руке, двери, баррикада) перезаливается через
  `upsertDynamic` каждый кадр в те же буферы.
- Свет: запечённый свет ламп (посчитан при сборке школы) × `lightMul` + конус фонаря в шейдере.
  Ночь — это `lightMul` 1 → 0.115 плюс смена цвета и плотности тумана.
- Адаптивное разрешение: если кадр > 20.5 мс — масштаб падает до 0.62, если < 12.5 мс — растёт до 1.

---

## 4. Состояния игры

```
menu → intro → play ⇄ hiding
                 ↓
               hurt → play
                 ↓
               dead
                 ↓
            (заново)
play → outro → won
```

- `intro` и `outro` полностью управляются `Timeline`: управление отключено, камера едет по ключам.
- `hiding` — камера внутри шкафчика, игрок невидим для ИИ, выход по `E`.
- `hurt` — пробуждение на полу класса: вставание, дыхание, 3.5 с неуязвимости.

### Вступительная кат-сцена (12 шагов)

1. Вход снаружи, двери открываются (ваш звук двери).
2. Затемнение в чёрный.
3. Чёрный экран + шёпот, субтитр.
4. Проявление: мы за партой в кабинете 101.
5. Учительница у доски пишет мелом (анимация руки + звук мела).
6. Стук в дверь — её зовут.
7. Она идёт к двери кабинета и выходит.
8. Тишина, камера смотрит на дверь.
9. **Крик** (ваш 3-й звук) + рывок камеры.
10. Экран гаснет.
11. **Дыхание** (ваш 4-й звук) в темноте, сердцебиение.
12. Подъём с парты → управление в руках игрока.

Пропуск — `Пробел` (шаг) или `Esc` (вся сцена).

---

## 5. ИИ учительницы

Состояния: `sleep → patrol → chase → attack → search → patrol`.

| Параметр | Значение |
| --- | --- |
| Скорость патруля / погони / поиска | 1.75 / 3.05 / 2.1 м/с |
| Зрение | 24 м, полуугол 1.15 рад (~66°) |
| Слух | 11 м, громкость зависит от бега/шага/приседания |
| Ощущение рядом | 3.6 м (видит даже со спины) |
| Удар | дистанция 1.85 м, замах 0.62 с, урон на 0.3 с, перезарядка 1.35 с |
| Память | гонится 6.5 с после потери цели, потом 7 с ищет |

Включённый фонарь увеличивает шанс быть замеченным; в шкафчике игрок невидим.
Передвижение — по графу узлов (`nav.ts`), поэтому она обходит стены и проходит в двери.
Модель собирается из коробок каждый кадр: корпус, платье, голова, волосы, руки, ноги,
дубинка и **самосветящиеся красные глаза** (эмиссия мимо общего света, видны в темноте).

---

## 6. Квест и предметы

| Предмет | Где | Зачем |
| --- | --- | --- |
| Гвоздодёр | спортзал | сорвать доски с двери |
| Кусачки | столовая | снять решётку |
| Ключ | подсобка за кабинетом 105 | открыть замок |
| Ручка | библиотека | вставить ручку |
| Предохранитель | у лестницы | подать питание на замок |
| Фонарь | кабинет 101 | свет ночью (`F`) |

- Взятие и любое действие с дверью — **удержание `E` 2.5 с** (`CONFIG.horror.holdSeconds`),
  с полосой прогресса; если отпустить — прогресс плавно сбрасывается.
- После взятия предмет показывается **в руке** с качанием при ходьбе, плюс подпись в HUD.
- Дверь выхода вскрывается строго по порядку этапов (`EXIT_STAGES`), прогресс виден на баррикаде.

---

## 7. Звук

- 29 mp3, суммарно 972 КБ, грузятся одним пакетом с прогрессом на экране загрузки.
- Циклы (гул школы, шаги, сердцебиение, удержание `E`) играют с плавными переходами;
  у mp3 обрезаны ~26 мс паддинга через `loopStart`/`loopEnd`, иначе слышны щелчки.
- Шаги учительницы — `playAt`: громкость и панорама считаются от позиции и поворота игрока,
  поэтому в шкафчике слышно, с какой стороны она проходит.
- Контекст создаётся только после действия пользователя и ставится на паузу вместе с игрой.

---

## 8. Известные ограничения

- **Внешнюю библиотеку кат-сцен подключить было невозможно**: в песочнице нет интернета,
  `npm install three` / GSAP и подобные не скачаются. Поэтому движок кат-сцен написан свой
  (`timeline.ts`, 173 строки): шаги с `duration`, `onEnter/onUpdate/onExit`, сглаживание, пропуск.
  Апгрейд на GSAP/Theatre.js позже возможен — интерфейс шагов совместим по смыслу.
- Нет теней от динамических источников света (только конус фонаря и запечённый свет).
- Второй этаж есть в виде лестницы, но не построен.
- Мобильного управления нет (только ПК, как и договаривались).
- Сохранения прогресса нет, сохраняются только настройки.

---

## 9. История версий

### 0.1.0 — каркас школы
Геометрия 12 помещений, коллизии, ходьба, миникарта, отладочный HUD.

### 1.0.0-beta — хоррор (текущая)
- Звук: 4 ваших файла + 25 синтезированных, менеджер WebAudio.
- Учительница: модель, анимация шагов/замаха, ИИ, красные глаза, дубинка.
- Две кат-сцены, 5 предметов, баррикада из 5 этапов, ночь, фонарь, 9 шкафчиков, 5 жизней.
- Новое меню и игровой HUD, настройки с сохранением.
- Исправления: типы цвета очистки в рендере (компиляция падала на `as const`),
  двойное умножение чувствительности мыши, пауза при потере фокуса окна,
  коллайдер стеклянного входа, конфликт `F` (фонарь против полного экрана),
  щелчки на стыках звуковых циклов.

### Следующий этап — онлайн
Состояние игры уже отделено от отрисовки, шаг физики фиксирован.
План: WebSocket-сервер на Node, авторитарная учительница на сервере, клиенты шлют ввод,
интерполяция чужих игроков по двум последним снимкам состояния.

## Патч 09.09.2026 — технические детали

### Геометрия дверей (`src/game/items.ts`)

- `buildExitDoors`: центр створки считается как `centerZ = z - (leafWidth / 2) * sin(angle)`, ручка — `z - (leafWidth - 0.18) * sin(angle)`. Раньше правая створка уходила в другую сторону и выворачивалась наружу.
- `buildClassDoor(mesh, angle)` — новая створка двери кабинета 101: петли (12.65, 12.125), ширина 1.1, `rotatedBox` вокруг Y.
- `buildChalkMarks(mesh, count)` — 12 меловых штрихов на плоскости x 11.2–11.225 (доска — x 11.125, z 3.2–8.6, y 1.02–2.26), открываются по `writePhase / 2`.

### Учительница (`src/entities/teacher.ts`)

- `buildMesh(mesh, { writing?, writePhase?, human? })`; `hostile = !writing && !(human ?? false)`.
- Глаза: `hostile ? EYE : [0.16, 0.14, 0.14]`, размер 0.05 / 0.035, `emissive` только в враждебном виде.
- Поза у доски: поднятая рука с мелом + журнал; ветка `if (!hostile) { … return }` не добавляет дубину.

### Таймлайн заставки (`src/game/game.ts`)

- Новые точки: `BOARD_SPOT {11.8, 5.9, π/2}`, `CLASS_DOOR {13.2, 11.35}`, `CLASS_DOOR_OUT {13.2, 13.1}`.
- Новые флаги: `itemsVisible`, `barricadeVisible`, `classDoorVisible`, `classDoorAngle`, `classDoorTarget`, `teacherHuman`.
- `updateWriting()` водит её вдоль доски (±0.42 по z), `yaw = BOARD_SPOT.yaw`, шаги выключены.
- Шаг `called`: позиция фиксируется, разворот через `mixAngle(π/2 → π)`.
- Шаг `leaves` (4.6 с): до 0.62 идёт к двери, дальше выходит в коридор; створка открывается с `progress ≥ 0.46` с одноразовым `door_open`; на выходе угол 0.1 и `click`.
- `scream.onEnter` → `teacherHuman = false` (глаза загораются именно здесь).
- `seatSway(lookAt?, weight?)`: желаемый угол — `atan2(-(tx - cx), -(tz - cz))`, смешивание `mixAngle`. В `leaves` вес растёт до 1 за 22 % шага, в `silence` падает до 0 за половину шага.
- `beginPlay()`: включает предметы/доски, открывает дверь кабинета (`classDoorTarget = 1.15`) с задержанным `door_open`.
- Динамические чанки: `teacher`, `items`, `held`, `exit`, `classdoor`, `chalk`.

### Захват курсора (`src/core/input.ts`, `src/main.ts`)

- `requestPointerLock(): Promise<boolean>` — пробует `unadjustedMovement`, при отказе повторяет без опций, все ошибки гасятся `catch`.
- В `main.ts` глобальный `unhandledrejection` глушит только отказы pointer lock, остальные ошибки по-прежнему видны.
- Если блокировка не взялась — `gameHud.toast("Щёлкните по экрану, чтобы взять мышь", 5)`.

### Как проверялось

Покадровые скриншоты в headless Chromium по `?autostart=1&nosound=1&warmup=N`: 5.5 (входные двери), 13 и 19 (доска и мел), 22.6 (разворот, глаза тёмные), 25.4 / 26.8 / 27.9 (уход и дверь), 29.6 (тишина), 45 (начало игры, ошибок нет). Компиляция: `npx tsc -p .` без ошибок.
